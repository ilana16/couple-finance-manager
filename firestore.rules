rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes (for development/testing)
    // TODO: Add proper authentication rules in production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
